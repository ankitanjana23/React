// this file import createContext from react and use it
// we need another which can handle contextProvider
import { createContext } from "react";

const UserContext = createContext();

export default UserContext 
//here we got a error first declare it than export 